status: verified
sources: [[raw/context-md-2026-06-11.pt.md]] (§10)
updated: 2026-09-06

# Changelog

Immutable record translated from the original Portuguese in [[raw/context-md-2026-06-11.pt.md]] (§10). New entries: one line per relevant change.

| Date | Change |
|---|---|
| 2026-04-27 | Initial [[raw/context-md-2026-06-11.pt]] creation |
| 2026-04-27 | Camada 1 partial: monitor + database integration, dedup by cod_alerta, pure function montar_alerta, unit tests (6 passing), GitHub repo |
| 2026-04-28 | Camada 1 fixes: error counter, UTF-8 encoding, sanity assertion for counters (7 tests passing) |
| 2026-04-28 | Exponential backoff retry in CEMADEN request, 4xx vs 5xx distinction (11 tests passing) |
| 2026-04-28 | Automatic scheduling with APScheduler (BackgroundScheduler), graceful Ctrl+C shutdown, requirements.txt (15 tests passing) |
| 2026-04-28 | **Camada 1 complete** — system runs continuously as service, resists network failures, cleans up on exit |
| 2026-04-29 | Camada 2 Part 1 complete: src layout refactoring, `pyproject.toml`, `alertavida` package 0.2.0 (15 tests passing) |
| 2026-05-01 | Camada 2 Part 2: 68 tests passing (revisions in issues #1 and #2). Claude Code installed, CLAUDE.md created. Data honesty principle formalized. AI agent workflow documented (§9) |
| 2026-05-01 | **Camada 2 complete** — Part 3 integrated, `pick_value()` removed, 68 tests passing |
| 2026-05-02 | Camada 3 — empirical wsAlertas2 inspection: status, ult_atualizacao and codibge fields mapped |
| 2026-05-02 | Camada 3 — database migration: lifecycle columns + eventos (outbox) table |
| 2026-05-02 | Camada 3 — pure ChangeDetector implemented, 79 tests passing |
| 2026-05-02 | Camada 3 — detector integration in executar_ingestao with transactional outbox, 81 tests |
| 2026-05-02 | **Camada 3 complete** — EventBus, OutboxDispatcher, scheduler job, 88 tests passing |
| 2026-05-03 | Automated test infrastructure: uv + uv.lock, pytest-cov/randomly/ruff, GitHub Actions CI (Ubuntu + Windows), conftest.py with db_temporario fixture, integration marker, daily CEMADEN contract test |
| 2026-05-04 | Pre-Camada 4 — multi-source ingestion design: roadmap renumbered (4-8 with new Camada 5 Correlation), architectural decisions on EscopoGeografico, surrogate key, global EONET ingestion, configurable bands. [[raw/context-md-2026-06-11.pt]] updated before code |
| 2026-05-05 | Pre-Camada 4 Parte A — empirical analysis of 4 CEMADEN samples (240 alerts, 01-02/05/2026) confirmed absence of COBRADE field and taxonomy limited to 2 physical types × 3 levels. Decisions: TipoEvento refactor to COBRADE subgroups; cobrade_codigo + FonteClassificacao on Alerta; granularity limited to subgroup in Camada 4; Parte A subdivided into A.1 (destructive) + A.2 (additive COBRADE); versioned CEMADEN fixtures in tests/fixtures/; data/* in gitignore. [[raw/context-md-2026-06-11.pt]] updated before code |
| 2026-05-07 | Camada 4 Parte A.1 — A.1.1, A.1.3 committed with CI green. A.1.2 committed LOCAL (not pushed). A.1.4 complete and committed. Push pending |
| 2026-05-09 | Camada 4 Parte A.1.4 complete — detector, database, monitor, tests and scripts/reclassificar_escopos.py. cod_alerta str, surrogate key + UNIQUE (fonte, cod_alerta), coordenadas required, escopo_geografico pre-computed at ingestion. 120 tests passing |
| 2026-05-11 | Camada 4 Parte A.2 complete — domain/cobrade.py with EVENTO_CEMADEN_PARA_COBRADE (2 entries based on empirical inspection) and validar_formato. FonteClassificacao enum. cobrade_codigo and fonte_classificacao fields on Alerta with validators. Corresponding columns via idempotent _migrar_banco(). 139 tests, CI green Ubuntu + Windows |
| 2026-05-12 | Item 8 of test cycle complete — direct database.py tests (11 tests covering compatibility check, additive migration, current schema creation, idempotence). Retroactive discovery: A.1 introduced schema break without automatic migration — SchemaIncompativelError added. Legacy schema fixtures versioned in tests/fixtures/schemas_legados.py. 150 tests, CI green |
| 2026-05-12 | Pre-Parte B — architectural design registered before implementation. Decisions documented in decision record |
| 2026-05-13 | Camada 4 Parte B.0 complete — fonte as Alerta attribute, propagated through stack (domain + infra). Two chained commits: B.0.a (domain, intentionally red CI) + B.0.b (infra, green CI). 183 tests, 0.58s |
| 2026-05-16 | Camada 4 Parte B.1 complete — DataSource interface + CemadenSource extraction + contract test infra. Two chained commits, both CI green. 205 tests |
| 2026-05-17 | Camada 4 Parte B.2.a complete — isolated orchestrator in ingestion/orquestrador.py. Three chained commits (B.2.a + hardening-1 + hardening-2), all CI green. 223 tests |
| 2026-05-17 | Camada 4 Parte B.2.b complete — monitor.py reduced to pure entrypoint (46 lines), scheduler.py adjusted. 222 tests. **Parte B inteira complete (8 chained commits, 8 green CIs, zero rollback)** |
| 2026-05-18 | Camada 4 Parte C.0.a — empirical NASA EONET v3 payload inspection. Report in docs/analise_eonet_2026-05-18.md (now [[raw/analise-eonet-2026-05-18.md]]). diagnostico_banco.py promoted to versioned utility. **Next: C.1 — implement NasaEonetSource** |
| 2026-06-11 | Bug 1 fix: resolved alert reappearing now emits `AlertaReativado` and reactivates the row (no more IntegrityError crash-loop). `buscar_snapshots_ativos` renamed to `buscar_snapshots` (returns all statuses). New counter `reativados` in `RelatorioFonte`. [[decisions/alert-reactivation-instead-of-crash]] |
| 2026-06-11 | Bug 2 fix: duplicate `cod_alerta` within the same source batch deduplicated (first kept, each duplicate increments `descartados`), eliminating runtime crash. Invariant 22 added |
| 2026-06-11 | Bug 3 fix: `_normalize_payload` raises `FalhaDeColeta` for unknown dict format or non-list/non-dict payload instead of silently returning []. Invariant 23 added |
| 2026-06-11 | Open-source preparation: Apache-2.0 license, package metadata (PEP 639), README, SECURITY.md, CI hardening (least-privilege permissions + lint), dependabot |
| 2026-06-11 | fix: restaura reconfigure UTF-8 e config de logging no entrypoint monitor |
| 2026-06-11 | fix: padroniza processado_em da outbox em UTC-aware |
| 2026-06-11 | feat: habilita WAL e busy_timeout no SQLite para concorrencia ingestao/dispatcher |
| 2026-06-11 | feat: scheduler loga relatorio por rodada via reporting.py compartilhado |
| 2026-06-14 | Camada 4 Parte C.1 complete — `NasaEonetSource` (second `DataSource`). Builds `Alerta` directly (not `from_dict`) due to v3 payload shape: GeoJSON `[lon, lat]`, no severity (`nivel_risco=INDETERMINADO`), category→`TipoEvento` via `CATEGORIA_EONET_PARA_TIPO`, most-recent-fix selection by date. Production query `status=open`. `cobrade_codigo` left None/INDETERMINADA (numeric COBRADE deferred to C.2). Synthetic fixtures in `tests/fixtures/eonet/`. Two chained commits (C.1.a fixtures + C.1.b source), reuses parametrized contract test. 252 tests, CI green |
| 2026-06-14 | Maintainability review Fase 1 — low-risk cleanup: `Alerta` imported under TYPE_CHECKING in orquestrador (dropped F821 ignore), dead `backports.zoneinfo` fallback removed, `_migrar_banco` docstring corrected, test-data layout documented. No behavior change |
| 2026-06-14 | Maintainability review Fase 2 / A1 — extracted shared HTTP transport to `sources/_http.py` (`fetch_com_retry` raising `FalhaDeColeta`, `parse_json`, `RespostaHTTP`/`Opener`). `CemadenSource`/`NasaEonetSource` lost ~50 duplicated lines each; retry tests consolidated into `tests/sources/test_http.py`. 253 tests |
| 2026-06-14 | Maintainability review Fase 2 / A2 — `aplicar_resultado_deteccao` uses `UPDATE … RETURNING id` via `_executar_retornando_id` (eliminates the per-event SELECT); ATUALIZADO and REATIVADO branches merged (REATIVADO adds `status_interno='ATIVO'`). No behavior change. 253 tests |
| 2026-06-21 | Camada 4 Parte C.2 complete — `EVENTO_EONET_PARA_COBRADE` dict + `mapear_eonet` in `domain/cobrade.py`; wired into `NasaEonetSource._montar_alerta`; `cobrade_codigo` / `fonte_classificacao` set per EONET category. 262 tests |
| 2026-06-21 | Camada 4 Parte C.3 complete — `NasaEonetSource()` added to source list in `monitor.py` and `scheduler.py`; two-source orchestration tests updated. 263 tests |
| 2026-07-02 | Maintainability review Fase 3 (issue #8) — dead schema fields removed: `AlertaSnapshot.nivel_risco`/`.tipo_evento` (never read by `detectar_mudancas`, `buscar_snapshots` SELECT trimmed to match), `EventoDetectado.schema_versao` (outbox INSERT already hardcoded `1`), and the `alertas.assinatura` column (always `NULL`, superseded by `ult_atualizacao`; dropped via idempotent `ALTER TABLE ... DROP COLUMN` in `_migrar_banco`). No behavior change. 290 tests |
| 2026-07-02 | Maintainability review issue #10 closed — Camada 4 C.3 verified complete (both sources wired, multi-source failure isolation tested, wiki already reflected EONET as integrated); issue was never closed after the work landed |
| 2026-07-02 | Maintainability review issue #11 (C1/C2/D3/D4) — deleted orphan `scripts/export_context.py` (predated the wiki, unreferenced) and `scripts/inspect_eonet_payload.py` (purpose served, Camada 4 C.1-C.3 complete); dropped speculative `idx_uf`/`idx_evento`/`idx_nivel` indexes (no query uses them; `idx_fonte`/`idx_escopo_geografico` kept); persisted `Alerta.descricao` (column + INSERT + event payload — was write-only despite `NasaEonetSource` populating it since C.1). 295 tests |
| 2026-07-02 | Maintainability review issue #18 (A/B/C) — removed `ResultadoDeteccao.codigos_resolvidos` (redundant with `eventos` filtered by `RESOLVIDO`, investigated wiring it in first and found no non-redundant use); documented `eventos.tentativas` as inert until a real retry policy exists (`wiki/components/events.md`); left `EventBus.handler_count` as-is (test-only utility, low impact). 295 tests |
| 2026-07-02 | **Bug fix (issue #30), found investigating #19** — every CEMADEN alert since Camada 4 A.2 was classified `tipo_evento=INDETERMINADO` / `cobrade_codigo=NULL`: `CemadenSource` read a nonexistent `tipoevento` key (real key is `evento`, compound `"<categoria> - <nível>"`), and even the right key's value never matched the bare-category mapping tables. `_categoria_do_evento` now splits correctly; test fixtures rewritten to the real payload shape (they previously tested a shape the API never sent); daily contract test hardened with a classification-rate assertion so acceptance-rate-only checks can't mask this again. Verified against the live CEMADEN endpoint. 298 tests |
| 2026-07-02 | SDD practices adopted from spec-kit evaluation (issue #33) — full tooling adoption rejected (dual sources of truth vs the wiki; see [[decisions/sdd-practices-from-spec-kit]]); five practices adopted as conventions: [[patterns/spec-checklist]] (quality gate against specs, born from #30), [[patterns/layer-convergence]] (declared-vs-real audit, born from #10/#30), `## Clarifications` section in layer pages (seeded in [[projects/layer-5-correlation]]), dependency/parallel markers for issue batches ([[patterns/ai-agent-workflow]]), explicit-skip rule. Docs only, no code |
| 2026-07-03 | Maintainability review issue #19 — pruned ~25 speculative aliases from `Alerta.from_dict` (`_pick` calls) down to the real CEMADEN key set confirmed in #30 (`cod_alerta`, `evento`, `nivel`, `latitude`, `longitude`, `datahoracriacao`, `ult_atualizacao`, `municipio`, `uf`, `codibge`); dropped the `descricao` pick entirely (no real sample ever had one). Simplified `CemadenSource._montar_alerta` — the #30 fallback branch became unreachable once the aliases it depended on were removed. Test fixtures renamed to the real shape across `test_alerta.py`/`test_detector.py`/`test_cemaden.py`; 3 tests repurposed into regressions asserting old aliases are now rejected; 1 fully-redundant test removed. Verified: full suite green, live CEMADEN contract test passed, exhaustive re-check of all 475 real sample items shows 0 discards/0 unclassified. 297 tests |
| 2026-07-17 | Maintainability review issue #37 (1st of the 2026-07-13 audit) — the four `agendar_ingestao()` tests in `test_scheduler.py` were the only DB-touching tests not isolating from the real database: they patched `BackgroundScheduler`/`time.sleep`/`sys.exit` but not `criar_banco()`, so every `uv run pytest` bootstrapped and migrated `data/alertavida.db` for real. Fixed by patching `alertavida.scheduler.criar_banco` in all four tests, plus a new autouse `_guarda_db_path` fixture in `conftest.py` that redirects `database.DB_PATH` to a per-test `tmp_path` by default (tests with their own `DB_PATH` monkeypatch, like `db_temporario`, override it unaffected). Verified empirically: `rm -rf data/ && uv run pytest` — suite green (297 tests), `data/alertavida.db` not created (empty `data/` mkdir side effect remains, tracked separately by #22). No `src/` changes |
| 2026-07-17 | Maintainability review issue #38 (2nd of the 2026-07-13 audit) — four zero-risk cleanups, one commit each: (A) `_pick` reduced to a single-key helper (`*keys` varargs was dead generality since #19's alias pruning — all 10 call sites already passed exactly one key); (B) removed the permanently-empty `biologico` set and its unreachable `if` in `TipoEvento.from_string` (`BIOLOGICO` enum value itself kept — it is COBRADE taxonomy, pending a real mapping); (C) `log_handler` now labels the surrogate id `agregado_id=` instead of the misleading `cod_alerta=`; (D) removed the pointless `eventos: list[EventoDetectado] = resultado.eventos` alias in `aplicar_resultado_deteccao` (iterates `resultado.eventos` directly) and the now-unused `EventoDetectado` import. No behavior change anywhere. 297 tests |
| 2026-07-17 | Maintainability review issue #39 (3rd of the 2026-07-13 audit) — HTTP transport hardening in `sources/_http.py`: (A) `fetch_com_retry` gained `max_resposta_bytes` (default `MAX_RESPOSTA_BYTES = 20 MB`), reads at most limit+1 bytes via `response.read(n)`, oversized body fails immediately with no retry (same treatment as 4xx) — closes an unbounded-memory-read gap; (B) new `opener_padrao` (default export, `build_opener(_RedirectHTTPSObrigatorioHandler)`) refuses any redirect to a non-https URL, closing an https→http downgrade path an attacker could use to inject fake alerts in plaintext — `CemadenSource`/`NasaEonetSource` now default their `opener` param to `opener_padrao` instead of bare `urlopen` (only change in those two files). Bundled fix: the retry conditional was tightened from `400 <= code < 500 and code not in (408, 429)` to an explicit retryable set (`500 <= code < 600 or code in (408, 429)`), so the now-reachable 3xx redirect-refusal fails immediately instead of retrying 4 times against a deterministic failure — brings the code in line with invariant 3's own text, no behavior change for previously-tested codes. `RespostaHTTP` Protocol grew `read(n=-1)` (was `read()`) to carry the cap through; not a new contract, `http.client.HTTPResponse.read(amt)` already supports it. Invariants 24/25 added. 303 tests |
| 2026-07-21 | Maintainability review issue #40 (4th of the 2026-07-13 audit) — `conectar()` turned into a `@contextlib.contextmanager` that yields the connection from inside a `with conexao:` transaction block and closes it in `finally`, fixing dozens of `ResourceWarning: unclosed database` (leaked to the GC on every `with conectar() as conexao:` call, since `sqlite3.Connection`'s own context manager only controls commit/rollback, never closes). The load-bearing rollback-on-exception behavior that keeps `aplicar_resultado_deteccao` atomic is preserved by construction — the `yield` sits inside the transactional `with`, so an exception in the caller is thrown back into it before the connection closes; call sites (`with conectar() as conexao:`) are unchanged. `scripts/reclassificar_escopos.py` switched from a raw `sqlite3.connect(DB_PATH)` (same leak) to the shared `conectar()`. New regression test asserts a mid-transaction exception in `aplicar_resultado_deteccao` rolls back both the `alertas` and `eventos` INSERTs. Same leak pattern (`with sqlite3.connect(...) as conexao:`, never closed) fixed in three test files' setup/verification helpers (`contextlib.closing()`) so the whole suite — not just production code — is warning-free. `uv run pytest -W always::ResourceWarning`: 0 unclosed-database warnings, 304 tests |
| 2026-07-21 | Maintainability review issue #41 (5th and last quick win of the 2026-07-13 audit) — `scripts/reclassificar_cobrade_cemaden.py` deleted, purpose fulfilled. It was the one-shot backfill for the #30 fix, meant to run once right after deploy; it ran (3/3 CEMADEN rows corrected, recorded when #30 closed / PR #32 merged), and there is no other environment left to backfill — alerts already outside the live feed were unreachable by design, so re-running it later gains nothing. Same precedent as `scripts/inspect_eonet_payload.py` (#11 C2). Git history preserves the script. No wiki reference named it before this removal; `wiki/decisions/decision-record.md` gained a matching row. No `src/`/`tests/` changes |
| 2026-07-22 | Maintainability review issue #20 (1st of the post-audit structural wave, after #37–#41) — `CemadenSource`/`NasaEonetSource` duplicated their `__init__` and `coletar()` skeleton line by line; new `HttpDataSource(DataSource)` in `sources/_http.py` consolidates both (shared keyword-only construction + `fetch_com_retry → parse_json → _normalize_payload → try/except ValueError → ResultadoColeta`), leaving each subclass with only `_normalize_payload`/`_montar_alerta` and `URL`/`USER_AGENT` class constants. Lives in `_http.py`, not `sources/base.py`, to avoid an import cycle and to keep the transport-agnostic `DataSource` ABC's promise intact — see [[decisions/template-method-http-datasource]]. Also extracted `domain/tempo.py::parse_iso_utc`, replacing 3 duplicated "parse ISO, assume UTC if naive" copies (`alerta.py` ×2, `nasa_eonet.py`'s `_fix_mais_recente`). Pure refactor, no behavior change — done ahead of INMET/INPE so the duplicated skeleton isn't copied two more times. 304 tests, both green in every commit |
| 2026-07-22 | Maintainability review issue #21 (2nd of the post-audit structural wave) — `agendar_ingestao()` swapped `BackgroundScheduler` + `while True: time.sleep(1)` keep-alive loop for `BlockingScheduler`, whose `start()` blocks the main thread and wakes only for jobs. Behavior identical item by item (`EVENT_JOB_ERROR` listener; `ingestao` 5min with immediate first run; `dispatcher` 30s; `max_instances=1`/`coalesce=True`/`misfire_grace_time=60` on both; clean `Ctrl+C` shutdown), minus the once-per-second busy wakeup and the manual loop + `sys.exit(0)` (dropped `time`/`sys` imports). Clean shutdown now via `try/except (KeyboardInterrupt, SystemExit)` around `start()` calling `shutdown(wait=False)`. Tests adapted to the blocking structure (patch `BlockingScheduler`, `start.side_effect=KeyboardInterrupt` for the shutdown path) while keeping #37's `criar_banco` patches; new `test_configuracao_dos_jobs` covers both jobs' anti-pile-up config. Verified manually: `python -m alertavida.scheduler` ran one immediate round (CEMADEN 50 / EONET 500) then shut down cleanly on SIGINT, no traceback. [[decisions/scheduler-background-jobs]]. 305 tests |
| 2026-07-28 | Maintainability review issue #22 (3rd of the post-audit structural wave) — persistence hardening in two parts. **A:** `DB_PATH` constant + import-time `mkdir` replaced by a `db_path()` function reading `ALERTAVIDA_DB_PATH` (blank/unset → packaged `data/alertavida.db`, same pattern as `ALERTAVIDA_BUFFER_PROXIMO_GRAUS`); `mkdir` moved into `conectar()`, so import has no filesystem side effect and `rm -rf data/ && uv run pytest` leaves nothing behind (closes the follow-up #37 left open). Scripts + test helpers switched from `setattr(DB_PATH)` to `setenv(ALERTAVIDA_DB_PATH)`. **B:** `eventos.agregado_id` gained a real `FOREIGN KEY` to `alertas.id` (`ON DELETE NO ACTION`, never cascade — the outbox is the audit trail) + child-key index `idx_eventos_agregado_id`, with `PRAGMA foreign_keys=ON` in `conectar()` (before the transaction block). No 12-step rebuild: `_verificar_compatibilidade_schema` now rejects pre-#22 databases (eventos without the FK) with `SchemaIncompativelError`, keeping `_migrar_banco` additive-only. Enforcement is per-connection (raw `sqlite3` connections stay unenforced by design — the FK protects nothing today, ids always derive from a same-transaction row; it is a cheap-now guardrail for Camada 6's API + outbox purge). Consequence: no real pre-#22 database can reach `_migrar_banco`'s A.1→A.2 branches any more — they are vestigial, and `TestMigrationAditiva` now runs against a deliberately synthetic fixture. Invariant 26 added. [[decisions/foreign-key-eventos-agregado-id]]. 309 tests |
| 2026-07-28 | Maintainability review issue #23 item A (mypy enforcement) — added `mypy` to the dev dependency-group and a `uv run mypy src/` step next to `ruff check .` in the `test` job, so type-checking runs on the full ubuntu + windows matrix; non-strict to start (the base is already fully annotated). Mechanical fixes to the 6 errors mypy surfaced: `_RedirectHTTPSObrigatorioHandler.redirect_request` had `fp`/`headers` widened to `object`, corrected to the parent's `IO[bytes]`/`HTTPMessage` so they pass through to `HTTPError`/`super()` type-clean (annotation-only); `apscheduler.*` (no stubs, no `py.typed`) scoped to `ignore_missing_imports` in `[tool.mypy]` rather than loosening the global check. The two known `# type: ignore[assignment]` on `opener_padrao` stay. No runtime change. 309 tests, mypy green |
| 2026-07-28 | Maintainability review issue #23 item B (E501 per-file-ignores dropped) — deleted the whole `[tool.ruff.lint.per-file-ignores]` section (5 files exempted from `E501`) and reflowed the 9 now-flagged long lines: implicit string-literal concatenation for the two single-line SQL strings (`events.py` UPDATE, `test_database.py` SELECT), paren-wrapped the two long expressions in `orquestrador.py` (`soma` sum, `criados_count` generator), and wrapped the `INSERT INTO eventos (...)` column lists (plus one long `VALUES`) inside the triple-quoted SQL in `test_events.py` (SQL whitespace is insignificant, and `# noqa` isn't even expressible inside those strings). `tests/sources/test_cemaden.py` had zero E501 lines — its exemption was already dead. Mechanical, behavior-preserving; no test intent changed. 309 tests, ruff clean with no per-file-ignores |
| 2026-07-30 | Follow-up to issue #23 item A — the mypy gate merged in #52 did not actually enforce type hints: in non-strict mode mypy neither requires annotations nor checks the bodies of unannotated functions, so a new function with no hints, a wrong-typed assignment and an undefined name inside it passed `uv run mypy src/` with exit 0 (mypy itself emitted the `annotation-unchecked` note). Enabled `disallow_untyped_defs` + `check_untyped_defs` in `[tool.mypy]`, which cost exactly 3 annotations across 2 files: the pydantic `mode="before"` validators `Municipio.normalize_uf`/`normalize_nome` (`value: Any -> Any` — pre-validation input really is arbitrary) and `scheduler._on_job_error` (`event: JobExecutionEvent`, apscheduler's real event class, which also documents the `.exception` the handler reads). `strict = true` remains future work (18 errors in 8 files, mostly bare `dict` in `events.py`). Verified by re-running the same probe: now 3 errors, exit 1. Annotation-only, no runtime change. 309 tests |
| 2026-07-30 | Maintainability review issue #24 (6th and **last** item of the 2026-07-13 audit) — `events.py` no longer ends in a module-level `bus = EventBus()` plus four import-time `subscribe()` calls; composition moved into `criar_bus_producao() -> EventBus`, which returns a fresh bus with `log_handler` on `AlertaCriado`/`AlertaAtualizado`/`AlertaResolvido`/`AlertaReativado`. `scheduler.py` (the singleton's only importer in the whole repo, confirmed by `grep -rn "import.*\bbus\b" src/ tests/ scripts/`) calls the factory once in `agendar_ingestao()` and injects the bus into `OutboxDispatcher`, held in a local so the factory never runs per tick. The module is now import-side-effect-free: `hasattr(alertavida.events, 'bus')` is `False` and its public names are exactly `EventBus`, `OutboxDispatcher`, `criar_bus_producao`, `log_handler`. Zero changes to existing tests — every occurrence in `test_events.py` already built its own `EventBus()`; two new tests assert the factory subscribes all four types and that two calls yield independent instances (a handler added to one is invisible to the other). Verified end to end with the exact scheduler wiring against a temp DB: a pending outbox row was processed and logged by the factory-registered handler (`[AlertaCriado] agregado_id=1 payload={'x': 1}`, `processados = 1`). **Done deliberately before Camada 8, which has not started** — [[projects/layer-8-notification]] stays `status: blocked`; the maintainer brought the work forward because the refactor is small and self-contained, and doing it while every handler still only logs is cheaper and safer than doing it mid-Camada-8 with real side-effecting handlers in play. [[decisions/outbox-pattern-eventbus]], [[components/events]]. 311 tests, ruff + mypy green |
| 2026-07-30 | Camada 5 clarification round 1 recorded in [[projects/layer-5-correlation]] — the structured round [[decisions/sdd-practices-from-spec-kit]] requires before technical planning, answering the 6 questions the page had been holding open since 2026-07-02. Layer stays `blocked`; this is spec work, no code. Decided: correlation is framed as record linkage (Fellegi–Sunter) with blocking → scoring → three-outcome decision including a review band (GDACS keeps expert supervision over merges/splits, so full auto-merge is not the target); the time window is asymmetric and derived from this project's own Δt distribution rather than from literature, instrumented before calibrated; the spatial predicate prefers the `codigo_ibge` administrative key, then point-in-polygon against the IBGE municipal mesh, with distance only as fallback, and bbox restricted to the blocking stage (filter-and-refine); severity is removed from the matching criteria entirely (correlation establishes identity, not agreement — `INDETERMINADO` means "no severity published", not "low"), and incident severity aggregates as `max`, never mean; the compatibility table is re-keyed on `cobrade_codigo` instead of the 5 coarse `TipoEvento` groups and splits identity from causal cascade into two relations; `Incidente` is the CAP 1.2 `<incidents>` concept (but inferred, not sender-declared, so memberships carry provenance), resolves only when all members resolve, must reactivate because alerts already do, and does not trust EONET's `closed`; correlation is forward-only with append-only merges, never re-evaluating closed incidents (O(open) not O(history), and incident ids must stay stable for Camada 8). Two items left explicitly OPEN and blocking: what a CEMADEN coordinate actually denotes (centroid vs. risk point — inferred from synthetic fixtures only, so unassertable per `_schema.md` rule 1) and whether a false merge or a false split is the worse error. Code consequence noted for implementation: `_fix_mais_recente` collapses the EONET geometry series to the latest observation, but correlation needs the earliest as onset |
| 2026-08-06 | Camada 5 clarification round 2 — closes the two items round 1 left OPEN, so nothing on [[projects/layer-5-correlation]] blocks technical planning any longer (layer stays `blocked` on its own prerequisites, not on unanswered questions). **CEMADEN coordinate semantics settled by measurement:** a live fetch returned one active alert nationwide (Uruguaiana/RS, codibge 4322400, `-29.840580 / -56.729935`), ~36 km from the municipal seat (29°45'17"S / 57°05'18"W) in a 5,702 km² municipality — so the coordinate is **not** the seat/urban centre, and the 12 decimal places indicate a computed value. This refutes round 1's reading of the test fixtures (Rio at `-22.91` ≈ city centre), which is exactly the inference `_schema.md` rule 1 forbids. Centroid-of-polygon vs. risk-point remains indistinguishable from one sample (Uruguaiana's seat is on the western border, so both candidates fall tens of km east); discriminator recorded — several alerts in one municipality, identical ⇒ centroid, varying ⇒ risk point — to re-run in the rainy season. It stopped blocking because either answer reinforces the round 1 preference for the `codigo_ibge` key and point-in-polygon over any radius: one point standing for 5,702 km² is ambiguous by tens of km regardless. **Maintainer's call decided: bias toward splitting (under-merging)** — duplicate incidents cost the user annoyance, a wrong merge can make a severe event inherit a mild one's label and lose attention; redundancy is cheap, omission is not. Recorded as a starting posture, not a final threshold: the three-outcome design flags the ambiguous band instead of deciding it, so thresholds get calibrated from accumulated flagged cases (same evidence-first discipline as the Q1 window), and revising it later moves thresholds, not architecture |
| 2026-08-07 | Camada 5 kickoff (issue #56) — the clarification rounds turned into an executable `## Technical plan` on [[projects/layer-5-correlation]], and the layer moved from `blocked` to `in-progress`: nothing blocks it any longer. Records the module layout (pure decision core in `domain/incidente.py` + `domain/correlacao.py`, mirroring how `ChangeDetector` was built pure before integration; state in `database.py`; wiring in `ingestion/orquestrador.py` right after `aplicar_resultado_deteccao`), the schema sketch (`incidentes` with the append-only `fundido_em` merge redirect and statuses mirroring `alertas.status_interno`; `incidente_membros` carrying provenance `score`/`motivo`/`criado_em` under `UNIQUE (alerta_id)`; `correlacao_observacoes` as #63's calibration dataset — all additive, FK style per #22), and the batch order with `Depends on:`/`Parallel-safe:` markers per [[patterns/ai-agent-workflow]]: #57 ∥ #58 → #59 → #60 → #61 → (#62 enhancement · #63 dormant), where #57 and #58 are the only parallelizable pair and #59/#60 are serial because both edit `database.py`. Six v1 scope decisions recorded explicitly: spatial predicate v1 is `codigo_ibge` equality with haversine fallback (point-in-polygon stays the recorded preference but ships as #62); every scoring constant is a provisional placeholder labelled as such in code — including the ±6h / 50 km inherited from the pre-clarification spec — with ties breaking toward splitting per round 2 and calibration owned by dormant #63; branch naming `camada5/<issue>-<slug>`; causal cascade out of v1 entirely; the review band has no interface in v1 (`correlacao_observacoes` *is* the queue); no new runtime dependency in v1. One open implementation question left deliberately undecided and owned by #59: incident lifecycle events cannot reuse `eventos.agregado_id` (FK to `alertas(id)` since #22), so the maintainer picks between payload-carried id, a nullable second column, or another design — the #22 FK is not to be weakened. Docs only, no code. 311 tests |
| 2026-08-07 | EONET onset (issue #57, 1st code change of Camada 5) — `NasaEonetSource` collapsed the EONET `geometry[]` time series to a single fix, so the alert's `data_criacao` was the **latest** observation, not the event onset; Camada 5 measures correlation windows against estimated onset ([[projects/layer-5-correlation]], Round 1 Q1), so this had to land before scoring. Verified first, then changed: `_fix_mais_recente` fed **both** `coordenadas` and `data_criacao`, and `ult_atualizacao` was hard-coded `None`. It is replaced by `_fixes_validos`, which returns the whole validated series in one pass; `_montar_alerta` then takes `data_criacao` from the **earliest** fix (onset) and `ult_atualizacao` + `coordenadas` from the **latest**, still selecting by date and never by list position. Position semantics unchanged; `ult_atualizacao` becoming a real timestamp is a second, deliberate behavior change — it makes EONET alerts eligible for `ATUALIZADO` detection for the first time (one `ATUALIZADO` event per already-open EONET alert on the first round after deploy, since stored snapshots hold `NULL`). Malformed fixes are still skipped, so a corrupt earliest fix cannot become the onset. 4 regression tests over the existing multi-fix fixture (earliest ≠ latest), asserting the three fields independently; the one pre-existing test that asserted `data_criacao` = latest encoded the bug and was retargeted at `ult_atualizacao`. 315 tests, ruff and mypy green |
| 2026-08-11 | Domain core for correlation (issue #58, 2nd code change of Camada 5, parallel-safe with #57) — new `domain/incidente.py` and `domain/correlacao.py`, both pure (no I/O, no SQL, mirrors how `ChangeDetector` was built pure before integration). `Incidente` (frozen pydantic, like `Alerta`) aggregates `MembroIncidente`s by the `(fonte, cod_alerta)` natural key, with `status`/`resolvido_em` under the same atomic-invariant style as `Alerta`'s `cobrade_codigo`/`fonte_classificacao` pair; `severidade` aggregates by **max** over members with known `nivel_risco`, `INDETERMINADO` members excluded but counted in `membros_sem_severidade` — the mean stays forbidden (Round 1, Q3). `correlacao.py` implements the record-linkage decision: `compatibilidade_tipo` compares identity at the most specific level available (COBRADE prefix, falling back to `TipoEvento`) over a diagonal-only table — `TipoEvento.INDETERMINADO` never counts as a match even against itself; `distancia_haversine_km` is a dependency-free geodesic helper (decision path never compares raw degrees, Round 1 Q2); `decidir_correlacao` scores available evidence (`codigo_ibge`, distance, time-to-onset, type compatibility) with two structural gates ahead of the score thresholds — incompatible types and indeterminate types can never reach `VINCULA`, regardless of future recalibration. All weights and thresholds are module-level constants, explicitly labelled provisional placeholders inherited from the pre-clarification spec; calibration stays owned by dormant issue #63. Causal cascade is out of scope by construction (docstring note only, per Round 1 Q4). New page [[components/domain-incidente-correlacao]]. 27 new pure unit tests covering all three outcomes, the two structural gates, and the distance helper against analytically-exact coordinate pairs. 342 tests, ruff and mypy green |
| 2026-08-11 | WeatherNext decision (issue #68) — Google DeepMind's WeatherNext 2 evaluated: **rejected as a Camada 4 `DataSource`** (gridded forecast fields, not discrete alerts — a source would have to invent `cod_alerta`/`nivel_risco`/COBRADE from thresholds, and the terms themselves state the data does not replace official agency alerts) and **adopted as a phased internal anticipation track** under the terms' unrestricted internal-use clause (§2a), with data older than 48 h plain CC BY 4.0. Phases: #69 offline skill validation over known Brazilian events (CC BY historic, **STOP gate** for the track — 0.25° ≈ 28 km vs. subgrid flash floods is an open empirical question), #70 surge watch in `scripts/` before the Oct–Mar rainy season (predicted surges trigger high-frequency CEMADEN capture for the Round 2 centroid-vs-risk-point discriminator, and coincide with `correlacao_observacoes` filling for #63), #71 physical-plausibility annotation for `REVISAO` labelling (human loop only, never a scoring input), #72 dormant public trend indicator gated on a written legal path (Non-Retrievable VAS or Maps Platform Weather API). Nothing touches the Camada 5 v1 critical path (#59→#60→#61). [[decisions/weathernext-anticipation-not-datasource]]. Docs only, no code |
| 2026-08-12 | WeatherNext licensing follow-up — question raised while filling the access request form ("what if this becomes commercial, e.g. B2G?"), answered from the terms and recorded on [[decisions/weathernext-anticipation-not-datasource]] so it is not re-derived later. Established: **there is no non-commercial clause** — the grant is royalty-free/revocable and silent on money, and historic data is CC BY 4.0 which permits commercial use explicitly; §3 restricts the *form* of what is shared (Non-Retrievable VAS publishable to anyone; Retrievable VAS only to identified known third parties for internal use; unmodified data never public), not whether it is paid. Consequence recorded: **B2G fits the terms better than a public consumer app**, since a municipal Defesa Civil is exactly §3's "clearly identified and known third party" case. The binding obstacle is §6, not the licence — as-is, not for consumer use, does not replace official alerts, Google's aggregate liability capped at USD 500 plus customer indemnity, which is a commercial problem under a paid civil-protection contract and points at the same two exits already gating #72 (written permission from weathernext@google.com, or the Maps Platform Weather API). No change to the current plan: #69/#70 are internal and untouched. Docs only, no code |
| 2026-08-12 | Incident persistence (issue #59, 3rd code change of Camada 5, serial after #58 — both edit `database.py`) — `criar_banco()` gains two new tables, `incidentes` (`status` ATIVO/RESOLVIDO mirroring `alertas.status_interno`, append-only self-referencing `fundido_em` merge redirect — Round 1 Q6, never a delete) and `incidente_membros` (`score`/`motivo`/`criado_em` provenance, `UNIQUE (alerta_id)` — an alert belongs to at most one live incident), both additive so `_verificar_compatibilidade_schema` needed no new rejection branch; every FK's child column indexed, plus a candidate-lookup index `idx_incidentes_status_fundido` for #60. **Design stop resolved before writing code** (mandatory per the issue): `eventos.agregado_id`'s FK to `alertas(id)` since #22 can't be reused for incident lifecycle events, so the maintainer chose a new nullable `agregado_incidente_id` column with its own FK over a payload-only reference or a second outbox table, on the condition that every incident event type name a real triggering alert (all five do, by construction — `agregado_id` stays `NOT NULL` and populated on incident events too) and that the resulting invariant (`agregado_incidente_id IS NOT NULL` iff the event is an incident lifecycle event) be validated in code rather than a `CHECK` constraint SQLite can't add via `ALTER TABLE`. Legacy databases receive the column additively via `_migrar_banco`'s `ALTER TABLE ADD COLUMN ... REFERENCES`, verified empirically to both work and enforce identically to a table-level FK. Five state-change functions (`criar_incidente`, `adicionar_membro_incidente`, `resolver_incidente`, `reativar_incidente`, `fundir_incidentes`) each write their state change and outbox event in one transaction (invariant 4); none decide *when* to fire — that's #60/#61. [[decisions/agregado-incidente-id]]. 34 new tests (schema, migration, FK enforcement, the runtime invariant, rollback atomicity, and per-function behavior including the merge redirect leaving both incident ids resolvable), 376 tests total, ruff and mypy green |
| 2026-08-12 | Correlation blocking (issue #60, 4th code change of Camada 5, serial after #59 — both edit `database.py`) — R-Tree availability proved empirically first, not assumed: `TestCapacidadeEspacialSQLite` checks `PRAGMA compile_options` and creates/queries a real `rtree` virtual table; both assertions passed in CI on **both** `ubuntu-latest` and `windows-latest` (PR #76), so the plain-indexed-columns fallback the issue allowed was not needed. `idx_alertas_espacial` (`USING rtree(id, min_lat, max_lat, min_lon, max_lon)`) indexes every alert as a degenerate point bbox, populated once at `CRIADO` (position never changes after) with an idempotent backfill in `_migrar_banco` for pre-existing rows. New append-only `correlacao_observacoes` table records one row per `(alerta_id, incidente_id)` pair evaluated — including `NAO_VINCULA` and the `incidente_id IS NULL` "no candidate at all" case — the dataset dormant issue #63 will calibrate from (Round 1, Q1). `avaliar_candidatos_correlacao()` wires it together: blocking (R-Tree bbox against a buffered position, joined to `incidentes` filtered to `status='ATIVO' AND fundido_em IS NULL`, then a per-type window check in Python) generates candidates, each evaluated through #58's pure `decidir_correlacao` — no decision logic reimplemented here. `JANELA_ABERTA_SEGUNDOS_POR_TIPO` holds `(janela_antes, janela_depois)` per type, not a single symmetric value — the issue mandates the asymmetric per-type *structure* exist in v1 even while both sides start equal (`domain.correlacao.JANELA_TEMPO_SEGUNDOS`), so #63 later edits only the constants table, never the comparison shape; `_buscar_incidentes_candidatos` checks `delta_t`'s sign against the matching side (`-janela_antes <= delta_t <= janela_depois`), not `abs(delta_t)`. Two v1 design calls recorded as their own decision pages: the spatial index covers `alertas`, not `incidentes` (the kickoff plan scoped #60 to add only the third table) [[decisions/rtree-spatial-index-blocking]] — whose buffer-in-degrees guarantee is noted as Brazil-latitude-only, since `NasaEonetSource` ingests globally without a bbox filter and the buffer under-selects above ~34° from the equator; a candidate incident with multiple members is represented by its most recently joined member for the pairwise comparison [[decisions/incidente-representante-blocking]], which also now documents the resulting temporal-drift sampling bias for #63 (an incident whose newest member fell outside the window produces no observation row at all, even if an older member would have been in range — absence of a row must not be read as absence of correlation). Does not mutate `incidentes`/`incidente_membros` and is not yet called by the ingestion pipeline — wiring plus acting on `VINCULA`/`REVISAO` is #61. 11 new tests (closed/merged incidents excluded, out-of-window excluded, in-bbox-and-window included, zero-candidate and incompatible-type observation rows, and two proving the window comparison respects `delta_t`'s sign), 387 tests total, ruff and mypy green |
| 2026-08-13 | Correlation wired into ingestion (issue #61, 5th and **last v1 code change** of Camada 5, serial after #60 — touches the pipeline) — `ingestion/orquestrador.py` now correlates `CRIADO`/`REATIVADO` alerts right after `aplicar_resultado_deteccao` persists each source's batch (Round 1, Q6 forward-only: join, open, or merge; closed incidents never re-evaluated). `_abrir_ou_juntar_incidente` runs `avaliar_candidatos_correlacao` (#60) and acts: a single `VINCULA` joins (`adicionar_membro_incidente`); zero `VINCULA` opens a new Incidente (`criar_incidente`) — even when `REVISAO` observations exist, since Round 2's bias to split means `REVISAO` never auto-links and forward-only leaves no fourth "stay unlinked" state; two or more distinct `VINCULA`s (the triggering alert corroborates two open Incidents at once) triggers a merge (`fundir_incidentes`, survivor = the older/lower-id one). `REATIVADO` splits in two: an alert with a pre-existing Incidente membership only reactivates it if it had resolved (Round 1, Q5) — blocking is not re-run — while one with no prior membership (a gap case) falls back to the CRIADO path. `RESOLVIDO` resolves its Incidente only when **every** member — including ones inherited through a merge, via a new `WITH RECURSIVE` merge-tree walk in `todos_membros_resolvidos` — is resolved, never on any single member. `ATUALIZADO` deliberately does not re-correlate: position/onset are the only fields blocking uses and are immutable after `CRIADO`, so re-running blocking on every update would almost always reproduce the same decision at O(open incidents) extra cost per round; a genuine event-category flip is an accepted v1 gap given the split bias and REVISAO's low cost. `aplicar_resultado_deteccao` (#22-era) gained a return value — `{cod_alerta: alerta_id}` for CRIADO/ATUALIZADO/REATIVADO/RESOLVIDO — since the wiring point needs exactly those ids right after persistence and the function already computes them (Tell, Don't Ask); three new read-only `database.py` helpers (`buscar_incidente_atual`, `status_incidente`, `todos_membros_resolvidos`) support the lifecycle decisions — a small, justified deviation from the kickoff plan's "files it touches" list, recorded in [[decisions/incident-lifecycle-wiring]] along with the merge-survivor rule and the REVISAO-opens-its-own-incident interpretation. `RelatorioFonte` gained four counters (`incidentes_criados`/`juntados`/`fundidos`/`revisao`, included in the existing failure-branch zero-check invariant) rendered by `formatar_relatorio`; incident resolution/reactivation are **not** counters, by the same precedent as alert resolution — both derived from absence/reappearance over time, verified via the outbox instead. 16 new tests: 6 end-to-end via `FakeDataSource` covering every acceptance scenario (two-source same event → one incident with severity = max via the real `Incidente.severidade`, incompatible types → two incidents, review-band → separate Incidente + flagged observation, resolve only on last member, reactivation, merge with both ids staying resolvable) plus 10 direct unit tests for the three new `database.py` helpers and the `aplicar_resultado_deteccao` return value. 403 tests total, ruff and mypy green. **Camada 5 convergence audit run and passed** (no drift found — see [[projects/layer-5-correlation]], "Layer convergence audit"); layer moves `in-progress` → `done`. #56–#61 close the v1 critical path; #62 (point-in-polygon upgrade) and #63 (calibration) remain open, dormant, and explicitly not v1-blocking |
| 2026-09-05 | Deploy: systemd service + backup timer (issue #78, Phase 1 — local WSL validation, cost zero) — new `deploy/` directory: `alertavida.service` (`Type=simple`, dedicated `alertavida` user, `WorkingDirectory=/opt/alertavida`, `ALERTAVIDA_DB_PATH=/var/lib/alertavida/alertavida.db` outside the git checkout, `Restart=on-failure`), `alertavida-backup.{service,timer}` (daily, WAL-safe, N-day retention) and `deploy/README.md`'s copy-pasteable runbook. **Design point resolved with the maintainer — option (c):** `KillSignal=SIGINT` in the unit **and** a real SIGTERM handler in `scheduler.py`, both now calling `scheduler.shutdown(wait=True)` (was `wait=False`) so `systemctl stop`/`restart` actually drains an in-flight ingestion round instead of racing [[decisions/incident-lifecycle-wiring]]'s permanent-orphan window; `TimeoutStopSec=360` sized from the worst-case two-source retry budget (~268s). Backup uses `sqlite3.Connection.backup()` (`deploy/backup.py`), not the `sqlite3` CLI — not assumed present on the deploy target, same WAL-safety guarantee. `UMask=0077` on both units closes a file-permission hygiene item (the dev-mode DB is `0644`, world-readable on a shared host). [[decisions/systemd-vps-deployment]]. Consequence for #87: closes the graceful-restart path for the orphan-alert window entirely; #87's remaining scope narrows to SIGKILL and in-loop crashes. The collector had been stopped 8 days (since 2026-08-28T12:50Z) when this was picked up — Phase 2 (provisioning an always-on host) is deferred to September 2026, a separate session. 405 tests, ruff and mypy green |
| 2026-09-05 | WeatherNext surge watch (issue #70, WeatherNext Phase B, unblocked by #69's qualified PASS) — `scripts/weathernext_surge_watch.py` runs once a day, queries the WeatherNext 2 **mean** table (never the ensemble — 189–191 GB/day vs. 4.19 GB/day) over the Brazil bbox for the next 48h via the `bq` CLI (`subprocess`, `--maximum_bytes_billed`), and writes a small local JSON artifact (`data/`, gitignored) with a deterministic threshold indicator (summed `total_precipitation_6hr`, provisional `SURGE_WATCH_THRESHOLD_MM_48H=50.0`) instead of the originally-scoped ensemble probability — a cost-driven substitution recorded with real numbers, not assumed. `scripts/weathernext_cemaden_capture.py` reads that artifact's `watch_mode` flag and, only while true, archives the CEMADEN painel's RAW payload (no domain parsing, no `ingestion/orquestrador.py` involvement) at 5-minute cadence into `data/weathernext_cemaden_captures/` — feeding the Round 2 centroide-vs-ponto-de-risco discriminator ([[projects/layer-5-correlation]]), which had zero municipalities with more than one CEMADEN alert as of the 2026-09-04 audit. Three design stops resolved with the maintainer, all recorded in [[decisions/weathernext-surge-watch-design]]: `bq` CLI over the Python client library (`--maximum_bytes_billed` as an un-refactorable-away CLI flag, plus zero new dependency tree); the whole track runs as the **maintainer**, not `alertavida` (corrects an earlier same-session assumption — the `bq` ADC credential lives under the maintainer's home, which `alertavida` cannot traverse, independent of which client was chosen), so the artifact and captures land in this checkout's own `data/`, not `/var/lib/alertavida`; and scheduling ships as **user systemd units** (`systemctl --user`, `%h` specifier) rather than system units, because no username or `/home/` path may appear in a versioned file (`grep -rn "/home/" deploy/` is empty by construction — the Phase 2 host has a different user). Real cost measured against the live project, not just the dry-run bound: 176,160,768 bytes (~176 MB) for the actual production query shape, ~57× under the 4.2 GB safety cap, ≈0.5% of the 1 TiB free tier over 30 days. Cost-guard trips (`OrcamentoExcedidoError`) are distinguished from real `bq` failures by matching the literal error text verified against the real project (`"exceeded limit for bytes billed"`) — a tripped guard is logged and skipped, a real failure propagates and fails the unit. 36 new tests (network and `subprocess.run` fully mocked), 446 tests total, ruff and mypy green |
| 2026-09-06 | Maintainability review issue #89 (lote A, item 5 of 7) — two speculative/redundant indexes dropped from `alertas`: `idx_escopo_geografico` (same #11 D3 criterion — no query filters by that column) and `idx_fonte` (redundant with `sqlite_autoindex_alertas_1`, whose leading column is already `fonte` via `UNIQUE (fonte, cod_alerta)`). `DROP INDEX IF EXISTS` added to `_migrar_banco()` alongside the #11 D3 removals, matching `CREATE INDEX` statements removed from `criar_banco()`. Removal of `idx_fonte` was gated on `EXPLAIN QUERY PLAN` evidence against a populated database (not the empty one a naive check would use, where the planner has no statistics to reason from): a WAL-safe backup copy (609 `alertas` rows) showed the planner falling back to `SEARCH alertas USING INDEX sqlite_autoindex_alertas_1 (fonte=?)` for both `fonte` values present, no table scan — see PR for the literal EQP output. `buscar_snapshots` return value unchanged. `scripts/diagnostico_banco.py` deleted — its "which schema version is this?" question (May 2026) is answered at runtime by `_verificar_compatibilidade_schema` today, with tests, and nothing in the repo imported or called the script; same precedent as `scripts/export_context.py`/`scripts/inspect_eonet_payload.py` (#11 C1/C2) |
